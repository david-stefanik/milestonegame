var config = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  physics: `arcade`,
  
  scene: {
      preload: preload,
      create: create,
      update: update
  }
};

var game = new Phaser.Game(config);

function preload ()
{this.load.image()
  this.load.image
  this.load.image
  this.load.image
}

function create ()
{this.add.image()
  this.add.image()
  this.add.image()
  this.add.image()
}

function update ()
{
}